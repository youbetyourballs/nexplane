apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "nexplane.fullname" . }}-config
  namespace: {{ .Values.global.namespace }}
  labels:
    {{- include "nexplane.labels" . | nindent 4 }}
data:
  REDIS_URL: {{ printf "redis://%s-redis:6379/0" (include "nexplane.fullname" .) | quote }}
