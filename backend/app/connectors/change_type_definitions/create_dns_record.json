{
  "change_type": "create_dns_record",
  "display_name": "Create Dns Record",
  "description": "Executes Create Dns Record operation.",
  "steps": [
    {
      "generic_action": "create_dns_record",
      "purpose": "execute",
      "required": true
    }
  ],
  "preflight_checks": [
    "connector_reachable"
  ],
  "verification_methods": [
    "api_check"
  ],
  "rollback_action": "create_dns_record",
  "rollback_connector_type": "active_directory"
}