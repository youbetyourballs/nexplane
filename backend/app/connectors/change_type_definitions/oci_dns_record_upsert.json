{
  "change_type": "oci_dns_record_upsert",
  "display_name": "Upsert OCI DNS Record",
  "steps": [
    {
      "generic_action": "oci_dns_record_upsert",
      "purpose": "execute",
      "required": true
    }
  ],
  "preflight_checks": [
    "connector_reachable",
    "asset_exists"
  ],
  "verification_methods": [
    "api_check"
  ]
}