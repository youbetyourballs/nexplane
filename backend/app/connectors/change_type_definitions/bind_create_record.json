{
  "change_type": "bind_create_record",
  "display_name": "Bind Create Record",
  "description": "Creates a DNS record in a BIND9 zone via RFC 2136 dynamic update.",
  "steps": [
    {
      "generic_action": "create_dns_record",
      "purpose": "execute",
      "required": true
    }
  ],
  "preflight_checks": [
    "connector_reachable"
  ],
  "verification_methods": [
    "api_check"
  ],
  "rollback_action": "delete_dns_record",
  "rollback_connector_type": "bind_dns"
}
