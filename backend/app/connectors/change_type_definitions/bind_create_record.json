{
  "change_type": "bind_create_record",
  "display_name": "Bind Create Record",
  "description": "Executes Bind Create Record operation.",
  "steps": [
    {
      "generic_action": "bind_create_record",
      "purpose": "execute",
      "required": true
    }
  ],
  "preflight_checks": [
    "connector_reachable"
  ],
  "verification_methods": [
    "api_check"
  ],
  "rollback_action": "bind_create_record",
  "rollback_connector_type": "bind_dns"
}