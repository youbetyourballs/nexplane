{
  "change_type": "wdac_audit",
  "display_name": "WDAC Audit Mode",
  "description": "Run Windows Defender Application Control in audit mode to capture policy violations",
  "steps": [
    {"generic_action": "wdac_audit", "purpose": "execute", "required": true}
  ],
  "preflight_checks": ["connector_reachable"],
  "verification_methods": ["api_check"],
  "rollback_action": "wdac_audit",
  "rollback_connector_type": "nexplane_agent"
}
