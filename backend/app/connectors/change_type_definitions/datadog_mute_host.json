{
  "change_type": "datadog_mute_host",
  "display_name": "Datadog Mute Host",
  "description": "Mute all monitors on a Datadog host. Rollback: unmute the same host.",
  "steps": [
    {
      "generic_action": "datadog_mute_host",
      "purpose": "execute",
      "required": true
    }
  ],
  "preflight_checks": [
    "connector_reachable"
  ],
  "verification_methods": [
    "api_check"
  ],
  "rollback_action": "unmute_host",
  "rollback_connector_type": "datadog"
}
