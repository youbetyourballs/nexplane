{
  "change_type": "ldap_disable_user",
  "display_name": "Disable LDAP/AD User",
  "description": "Disables a user account in Active Directory. Rollback re-enables the account.",
  "steps": [
    {
      "generic_action": "disable_account",
      "purpose": "execute",
      "required": true
    }
  ],
  "preflight_checks": [
    "connector_reachable"
  ],
  "verification_methods": [
    "api_check"
  ]
}
