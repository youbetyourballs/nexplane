{
  "change_type": "update_reference",
  "display_name": "Update Reference",
  "description": "Update a specific reference to a target asset, credential, or identifier with a new value, based on a prior scan_for_references result.",
  "steps": [
    {"generic_action": "update_reference", "purpose": "execute", "required": true}
  ],
  "preflight_checks": ["connector_reachable", "asset_exists"],
  "verification_methods": ["api_check"],
  "rollback_action": "restore_original_reference"
}
