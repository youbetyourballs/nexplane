{
  "change_type": "datadog_send_event",
  "display_name": "Datadog Send Event",
  "description": "Post a custom event to the Datadog event stream. No rollback (events are immutable).",
  "steps": [
    {
      "generic_action": "datadog_send_event",
      "purpose": "execute",
      "required": true
    }
  ],
  "preflight_checks": [
    "connector_reachable"
  ],
  "verification_methods": [
    "api_check"
  ],
  "rollback_action": null,
  "rollback_connector_type": null
}
