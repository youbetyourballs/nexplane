{
  "change_type": "bind_list_zone",
  "display_name": "Bind List Zone",
  "description": "Lists all records in a BIND9 DNS zone via AXFR transfer.",
  "steps": [
    {
      "generic_action": "list_dns_records",
      "purpose": "execute",
      "required": true
    }
  ],
  "preflight_checks": [
    "connector_reachable"
  ],
  "verification_methods": [
    "api_check"
  ],
  "rollback_action": null,
  "rollback_connector_type": "bind_dns"
}
