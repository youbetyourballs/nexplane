{
  "change_type": "rotate_redis_password",
  "display_name": "Rotate Redis Password",
  "description": "Rotates the password for a redis user account.",
  "steps": [{"generic_action": "rotate_redis_password", "purpose": "execute", "required": true}],
  "preflight_checks": ["connector_reachable"],
  "verification_methods": ["api_check"],
  "rollback_action": "rotate_redis_password",
  "rollback_connector_type": "redis"
}
