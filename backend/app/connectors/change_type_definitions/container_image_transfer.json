{
  "change_type": "container_image_transfer",
  "display_name": "Cross-Cloud Container Image Transfer",
  "steps": [
    {
      "generic_action": "preflight_check",
      "purpose": "plan",
      "required": true,
      "description": "Verify source image exists and check if destination tag already exists"
    },
    {
      "generic_action": "transfer_image",
      "purpose": "execute",
      "required": true,
      "description": "Transfer container image from source registry to destination registry"
    }
  ],
  "preflight_checks": ["asset_exists"],
  "verification_methods": ["output_check"],
  "auto_approve": false,
  "rollback_command": "delete_transferred_image",
  "dry_run_stops_after_step": 1,
  "rollback_capability": "full"
}
