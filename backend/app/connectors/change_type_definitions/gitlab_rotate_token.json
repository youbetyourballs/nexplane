{
  "change_type": "gitlab_rotate_token",
  "display_name": "Gitlab Rotate Token",
  "description": "Executes Gitlab Rotate Token operation.",
  "steps": [
    {
      "generic_action": "gitlab_rotate_token",
      "purpose": "execute",
      "required": true
    }
  ],
  "preflight_checks": [
    "connector_reachable"
  ],
  "verification_methods": [
    "api_check"
  ],
  "rollback_action": "gitlab_rotate_token",
  "rollback_connector_type": "gitlab"
}