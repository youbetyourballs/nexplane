{
  "change_type": "seccomp_learn",
  "display_name": "Seccomp Learn (Observe Syscalls)",
  "steps": [
    {"generic_action": "seccomp_learn", "purpose": "execute", "required": true}
  ],
  "preflight_checks": ["connector_reachable", "asset_exists"],
  "verification_methods": ["api_check"],
  "rollback_action": "seccomp_learn",
  "rollback_connector_type": "nexplane_agent"
}
