{
  "change_type": "target_group_create",
  "display_name": "Create Target Group",
  "steps": [
    {"generic_action": "create_target_group", "purpose": "execute", "required": true}
  ],
  "preflight_checks": ["connector_reachable"],
  "verification_methods": ["api_check"],
  "rollback_action": "delete_target_group",
  "rollback_connector_type": "aws"
}
