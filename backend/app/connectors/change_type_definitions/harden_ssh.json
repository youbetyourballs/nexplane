{
  "change_type": "harden_ssh",
  "display_name": "Harden SSH",
  "steps": [
    {"generic_action": "harden_ssh", "purpose": "execute", "required": true}
  ],
  "preflight_checks": ["connector_reachable", "asset_exists"],
  "verification_methods": ["api_check"]
}
