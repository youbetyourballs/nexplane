{
  "change_type": "server_capture",
  "display_name": "Full Forensic Capture (AMI + Live Metadata)",
  "steps": [
    {"generic_action": "server_capture", "purpose": "execute", "required": true}
  ],
  "preflight_checks": ["connector_reachable", "asset_exists"],
  "verification_methods": ["api_check"]
}
