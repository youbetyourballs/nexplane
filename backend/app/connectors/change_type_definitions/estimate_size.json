{
  "change_type": "estimate_size",
  "display_name": "Estimate Size",
  "steps": [
    {"generic_action": "estimate_size", "purpose": "execute", "required": true}
  ],
  "preflight_checks": ["connector_reachable", "asset_exists"],
  "verification_methods": ["api_check"]
}
