{
  "change_type": "rotate_jwt_signing_key",
  "display_name": "Rotate Jwt Signing Key",
  "description": "Executes Rotate Jwt Signing Key operation.",
  "steps": [
    {
      "generic_action": "rotate_jwt_signing_key",
      "purpose": "execute",
      "required": true
    }
  ],
  "preflight_checks": [
    "connector_reachable"
  ],
  "verification_methods": [
    "api_check"
  ],
  "rollback_action": "rotate_jwt_signing_key",
  "rollback_connector_type": "aws"
}