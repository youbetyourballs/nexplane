{
  "change_type": "rotate_mongodb_password",
  "display_name": "Rotate Mongodb Password",
  "description": "Rotates the password for a mongodb user account.",
  "steps": [{"generic_action": "rotate_mongodb_password", "purpose": "execute", "required": true}],
  "preflight_checks": ["connector_reachable"],
  "verification_methods": ["api_check"],
  "rollback_action": "rotate_mongodb_password",
  "rollback_connector_type": "mongodb"
}
