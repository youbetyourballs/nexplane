{
  "change_type": "server_snapshot",
  "display_name": "Server Snapshot (AMI)",
  "steps": [
    {"generic_action": "server_snapshot", "purpose": "execute", "required": true}
  ],
  "preflight_checks": ["connector_reachable", "asset_exists"],
  "verification_methods": ["api_check"]
}
