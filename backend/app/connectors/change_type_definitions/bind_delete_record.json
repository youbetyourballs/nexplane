{
  "change_type": "bind_delete_record",
  "display_name": "Bind Delete Record",
  "description": "Deletes a DNS record from a BIND9 zone via RFC 2136 dynamic update.",
  "steps": [
    {
      "generic_action": "delete_dns_record",
      "purpose": "execute",
      "required": true
    }
  ],
  "preflight_checks": [
    "connector_reachable"
  ],
  "verification_methods": [
    "api_check"
  ],
  "rollback_action": "create_record",
  "rollback_connector_type": "bind_dns"
}
