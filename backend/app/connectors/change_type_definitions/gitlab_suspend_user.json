{
  "change_type": "gitlab_suspend_user",
  "display_name": "Gitlab Suspend User",
  "description": "Executes Gitlab Suspend User operation.",
  "steps": [
    {
      "generic_action": "gitlab_suspend_user",
      "purpose": "execute",
      "required": true
    }
  ],
  "preflight_checks": [
    "connector_reachable"
  ],
  "verification_methods": [
    "api_check"
  ],
  "rollback_action": "gitlab_suspend_user",
  "rollback_connector_type": "gitlab"
}