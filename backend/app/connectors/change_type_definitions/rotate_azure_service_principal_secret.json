{
  "change_type": "rotate_azure_service_principal_secret",
  "display_name": "Rotate Azure Service Principal Secret",
  "description": "Creates a new Azure service principal client secret and removes all old secrets.",
  "steps": [
    {"generic_action": "rotate_azure_service_principal_secret", "purpose": "execute", "required": true}
  ],
  "preflight_checks": ["connector_reachable"],
  "verification_methods": ["api_check"],
  "rollback_action": "rotate_azure_service_principal_secret",
  "rollback_connector_type": "nexplane_agent"
}
