{
  "change_type": "rotate_vault_secret",
  "display_name": "Rotate Vault Secret",
  "description": "Generates a new value for a field in a HashiCorp Vault KV secret.",
  "steps": [{"generic_action": "rotate_vault_secret", "purpose": "execute", "required": true}],
  "preflight_checks": ["connector_reachable"],
  "verification_methods": ["api_check"],
  "rollback_action": "rotate_vault_secret",
  "rollback_connector_type": "vault"
}
