{
  "change_type": "enforce_mfa",
  "display_name": "Enforce Mfa",
  "description": "Executes Enforce Mfa operation.",
  "steps": [
    {
      "generic_action": "enforce_mfa",
      "purpose": "execute",
      "required": true
    }
  ],
  "preflight_checks": [
    "connector_reachable"
  ],
  "verification_methods": [
    "api_check"
  ],
  "rollback_action": "enforce_mfa",
  "rollback_connector_type": "aws"
}