{
  "change_type": "rotate_ssh_keys",
  "display_name": "Rotate SSH Keys",
  "description": "Removes an old SSH public key by fingerprint and adds a new public key in authorized_keys on target hosts.",
  "steps": [
    {"generic_action": "rotate_ssh_keys", "purpose": "execute", "required": true}
  ],
  "preflight_checks": ["connector_reachable"],
  "verification_methods": ["api_check"],
  "rollback_action": "rotate_ssh_keys",
  "rollback_connector_type": "nexplane_agent"
}
