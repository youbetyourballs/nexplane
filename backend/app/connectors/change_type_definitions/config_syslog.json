{
  "change_type": "config_syslog",
  "display_name": "Configure Syslog",
  "steps": [
    {"generic_action": "config_syslog", "purpose": "execute", "required": true}
  ],
  "preflight_checks": ["connector_reachable", "asset_exists"],
  "verification_methods": ["api_check"]
}
