{
  "change_type": "rotate_postgres_password",
  "display_name": "Rotate Postgres Password",
  "description": "Rotates the password for a postgres user account.",
  "steps": [{"generic_action": "rotate_postgres_password", "purpose": "execute", "required": true}],
  "preflight_checks": ["connector_reachable"],
  "verification_methods": ["api_check"],
  "rollback_action": "rotate_postgres_password",
  "rollback_connector_type": "postgres"
}
