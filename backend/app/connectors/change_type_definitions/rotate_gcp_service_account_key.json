{
  "change_type": "rotate_gcp_service_account_key",
  "display_name": "Rotate GCP Service Account Key",
  "description": "Creates a new GCP service account key and deactivates all old user-managed keys.",
  "steps": [
    {"generic_action": "rotate_gcp_service_account_key", "purpose": "execute", "required": true}
  ],
  "preflight_checks": ["connector_reachable"],
  "verification_methods": ["api_check"],
  "rollback_action": "rotate_gcp_service_account_key",
  "rollback_connector_type": "nexplane_agent"
}
