{
  "change_type": "wdac_enforce",
  "display_name": "WDAC Enforce Mode",
  "description": "Enable Windows Defender Application Control in enforce mode",
  "steps": [
    {"generic_action": "wdac_enforce", "purpose": "execute", "required": true}
  ],
  "preflight_checks": ["connector_reachable"],
  "verification_methods": ["api_check"],
  "rollback_action": "wdac_enforce",
  "rollback_connector_type": "nexplane_agent"
}
