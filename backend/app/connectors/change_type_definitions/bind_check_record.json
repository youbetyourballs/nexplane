{
  "change_type": "bind_check_record",
  "display_name": "Bind Check Record",
  "description": "Queries a specific DNS record from a BIND9 zone and returns its current value.",
  "steps": [
    {
      "generic_action": "check_dns_record",
      "purpose": "execute",
      "required": true
    }
  ],
  "preflight_checks": [
    "connector_reachable"
  ],
  "verification_methods": [
    "api_check"
  ],
  "rollback_action": null,
  "rollback_connector_type": "bind_dns"
}
