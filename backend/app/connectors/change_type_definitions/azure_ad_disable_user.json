{
  "name": "azure_ad_disable_user",
  "display_name": "Disable Azure AD User",
  "description": "Disables an Azure AD / Entra ID user account and revokes all active sessions via Microsoft Graph API.",
  "category": "identity",
  "agent_required": false,
  "parameters": {
    "user_identifier": {"type": "string", "description": "User Principal Name (UPN) or object ID", "required": true}
  }
}
