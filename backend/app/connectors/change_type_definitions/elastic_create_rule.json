{
  "change_type": "elastic_create_rule",
  "display_name": "Elastic Create Rule",
  "description": "Executes Elastic Create Rule operation.",
  "steps": [
    {
      "generic_action": "elastic_create_rule",
      "purpose": "execute",
      "required": true
    }
  ],
  "preflight_checks": [
    "connector_reachable"
  ],
  "verification_methods": [
    "api_check"
  ],
  "rollback_action": "elastic_create_rule",
  "rollback_connector_type": "elastic"
}