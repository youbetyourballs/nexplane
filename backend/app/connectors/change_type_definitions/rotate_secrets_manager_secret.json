{
  "change_type": "rotate_secrets_manager_secret",
  "display_name": "Rotate Secrets Manager Secret",
  "description": "Generates a new secret value and updates AWS Secrets Manager. Old version retained for rollback.",
  "steps": [
    {"generic_action": "rotate_secrets_manager_secret", "purpose": "execute", "required": true}
  ],
  "preflight_checks": ["connector_reachable"],
  "verification_methods": ["api_check"],
  "rollback_action": "rotate_secrets_manager_secret",
  "rollback_connector_type": "nexplane_agent"
}
