{
  "change_type": "apply_sysctl_hardening",
  "display_name": "Apply Sysctl Hardening",
  "steps": [
    {"generic_action": "apply_sysctl_hardening", "purpose": "execute", "required": true}
  ],
  "preflight_checks": ["connector_reachable", "asset_exists"],
  "verification_methods": ["api_check"]
}
