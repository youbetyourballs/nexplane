{
  "change_type": "configure_ntp",
  "display_name": "Configure NTP",
  "steps": [
    {"generic_action": "configure_ntp", "purpose": "execute", "required": true}
  ],
  "preflight_checks": ["connector_reachable", "asset_exists"],
  "verification_methods": ["api_check"]
}
