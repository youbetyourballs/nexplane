{
  "change_type": "authorized_keys_audit",
  "display_name": "Authorized Keys Audit",
  "steps": [
    {"generic_action": "authorized_keys_audit", "purpose": "execute", "required": true}
  ],
  "preflight_checks": ["connector_reachable", "asset_exists"],
  "verification_methods": ["api_check"]
}
