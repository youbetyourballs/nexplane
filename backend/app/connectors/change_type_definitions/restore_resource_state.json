{
  "change_type": "restore_resource_state",
  "display_name": "Restore Resource State",
  "steps": [
    {"generic_action": "restore_drift_state", "purpose": "execute", "required": true}
  ],
  "preflight_checks": ["connector_reachable", "asset_exists"],
  "verification_methods": ["api_check"]
}
