# release.yml — edit before running: git tag v1.2.3 && git push --tags
severity: recommended        # recommended | security | critical
min_compatible_version: 1.0.0
changelog_url: https://docs.nexplane.ai/changelog/1.2.0
notes: "Feature release: expanded AMI smoke test suite (14 phases) covering all seeded accounts, frontend routes, demo/non-demo mode, MCP protocol, and systemd resilience; auto-selects SSH key-pair or password auth by IP type"
